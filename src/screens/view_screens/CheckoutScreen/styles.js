import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        paddingLeft: 10
    },
    header:{
        marginVertical:15,
        alignSelf:'center',
        fontSize: 18,
        fontWeight: 'bold'
    }
});

export default styles;