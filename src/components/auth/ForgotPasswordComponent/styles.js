import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    forgotContainer: {
        width: '100%',
        alignItems: 'flex-end',
        marginBottom: 24,
    },
});

export default styles;